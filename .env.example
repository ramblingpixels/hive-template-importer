# Supabase: Project settings -> Database -> Connection string -> URI.
# Use the transaction pooler URL for Vercel/serverless deployments.
DATABASE_URL=postgresql://postgres.PROJECT_REF:PASSWORD@REGION.pooler.supabase.com:6543/postgres

# Set false only for a trusted local PostgreSQL instance without TLS.
DATABASE_SSL=true

